# requirements.txt — core runtime dependencies for the Adapt stack.
#
# This is the minimal set needed to run the MPPI controller and
# diffusion predictor. For the full GPU training/inference env,
# use adapt_requirements.text instead. For the CPU-only dev env,
# use cs588_requirements.txt.
#
# Install:
#   conda create -n adapt python=3.12 -y
#   conda activate adapt
#   pip install -r requirements.txt
#
# ROS 2 (rclpy, message packages, etc.) comes from apt — not pip.
# Source /opt/ros/<distro>/setup.bash before running any ROS nodes.

# --- PyTorch (GPU) ---
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.5.1+cu121

# --- MPPI controller ---
pytorch_mppi>=0.9.1

# --- Core numerical ---
numpy>=1.26,<2.2
matplotlib

# --- Diffusion model training ---
pyarrow>=14.0

# --- Colcon build deps ---
empy==3.3.4
lark>=1.1
catkin_pkg>=1.0

# --- Dev ---
ipython
pytest
