# ROS 1 (rospy) flat YAML — load with <rosparam command="load" ns="sensor_fusion_node"
#   file="$(find adapt_full)/config/sensor_fusion_params_ros1.yaml"/>

matching_threshold: 2.0
lidar_distance_weight: 0.8
camera_distance_weight: 0.2
lidar_direction_weight: 0.3
camera_direction_weight: 0.7
sync_queue_size: 10
sync_slop: 0.1
